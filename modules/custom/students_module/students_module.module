<?php

function students_module_theme($existing, $type, $theme, $path) {

  return [
    // Name of the theme hook. This is used in the controller to trigger the hook.
    'students' => [
      'render element' => 'studentsTable',
    ],
    'students_add_form' => [
      'render element' => 'form',
    ],
  ];

}
